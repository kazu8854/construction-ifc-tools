{
  "name": "construction-ifc-tools",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:mock": "concurrently \"npm run dev:mock -w packages/backend\" \"npm run dev:mock -w packages/frontend\"",
    "dev:aws": "concurrently \"npm run dev:aws -w packages/backend\" \"npm run dev:aws -w packages/frontend\"",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "deploy": "npm run build && npm run deploy --workspace=infrastructure",
    "prepare": "husky",
    "secretlint": "secretlint '**/*'"
  },
  "lint-staged": {
    "*": [
      "secretlint"
    ]
  },
  "devDependencies": {
    "@secretlint/core": "^11.4.0",
    "@secretlint/secretlint-rule-preset-recommend": "^11.4.0",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "secretlint": "^11.4.0"
  }
}
